baseURL = "/"
languageCode = "en-au"
title = "Lincoln M. Tracy, PhD"
theme = "mainroad"
paginate = "10" # Number of posts per page

[Author]
  name = "Lincoln Tracy"
  bio = "Lincoln Tracy is a researcher and freelance writer based in Melbourne, Australia."
  #avatar = "img/avatar.png"

[Params]
  description = "Lincoln Tracy - Researcher and freelance writer" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  pager = true
  post_meta = ["date", "categories"] # Order of post meta information

[Params.logo]
  subtitle = "Researcher and Freelance Writer" # Logo subtitle

[Params.style.vars]
  highlightColor = "#f15c22"

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist"]

  [Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)